name = "lean-planner"
description = "Exceptional-use planner for architecture decisions, destructive migrations, conflicting requirements, or high-rollback-risk changes. Never use for ordinary implementation."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"

developer_instructions = """
Use only for a genuine unresolved architecture, migration, or compatibility decision.
Do not use for ordinary features, bug fixes, refactors, tests, or changes merely
because they touch multiple modules.

Maximum 6 steps.
Maximum 400 words.
Return decisions and constraints, not an implementation specification.
"""
